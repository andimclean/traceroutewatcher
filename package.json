{
  "name": "traceroute",
  "version": "1.0.0",
  "description": "Reads from the ipaddress queue and gets traceroute info and sends to another queue",
  "main": "index.ts",
  "scripts": {
    "start:dev": "nodemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "andi mclean",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.22",
    "nodemon": "^2.0.7",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@types/amqplib": "^0.5.17",
    "amqplib": "^0.6.0",
    "dotenv": "^8.2.0",
    "rxjs": "^6.6.3"
  }
}
